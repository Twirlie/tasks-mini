---
title: "Domain types and validation"
status: "ready-for-human"
created: "2026-05-10T07:25:00Z"
updated: "2026-05-10T08:07:00Z"
labels: ["mvp", "enhancement", "ready-for-human", "module:domain", "completed"]
---

## Parent

MVP PRD: `.scratch/mvp/prd.md`

## What to build

Define the core domain types (`Task`, `Column`, `Board`) with validation rules. This is a pure Rust module with no I/O dependencies. Implement business invariants like unique column names and non-negative task ordering. Include comprehensive unit tests.

## Acceptance criteria

- [x] `Task` type with: id, title, description, column_id, order, created_at, updated_at, completed_at
- [x] `Column` type with: id, name, order
- [x] `Board` type with: id, name, columns, schema_version
- [x] Validation: title max 200 chars, description max 2000 chars
- [x] Validation: column names unique per board, max 50 chars
- [x] Validation: task order non-negative integers
- [x] Unit tests for all types and validation rules
- [x] Tests for edge cases: empty strings, boundary values

## Blocked by

None - can start immediately

---

> *This was generated by AI during triage.*

## Agent Brief

**Goal:** Create the `domain` module in `src-tauri/src/domain/` with core types and validation.

**Types to define:**
- `Task` — id (String/UUID), title (String), description (Option<String>), column_id (String), order (u32), created_at (DateTime<Utc>), updated_at (DateTime<Utc>), completed_at (Option<DateTime<Utc>>)
- `Column` — id (String/UUID), name (String), order (u32)
- `Board` — id (String/UUID), name (String), columns (Vec<Column>), schema_version (u32)

**Validation rules:**
- Task title: required, max 200 chars
- Task description: optional, max 2000 chars
- Column name: required, max 50 chars, unique per board
- Task order: non-negative integers (u32 satisfies this by construction)

**Implementation notes:**
- Pure Rust module — no I/O, no trait dependencies
- Use `thiserror` for `DomainError` enum (Validation, NotFound, DuplicateColumnName)
- All types derive `Serialize`, `Deserialize`, `Clone`, `Debug`
- Use `chrono::DateTime<Utc>` for timestamps
- Use `uuid::Uuid` for IDs (or String with helper to generate)
- Add `Board::validate()` method that checks all invariants
- Add `Task::new()` and `Column::new()` constructors that validate inputs

**Dependencies to add to `src-tauri/Cargo.toml`:**
- `chrono = { version = "0.4", features = ["serde"] }`
- `uuid = { version = "1", features = ["v4", "serde"] }`
- `thiserror = "2"`

**Tests:** `#[cfg(test)]` module in same file. Cover validation rules, edge cases (empty strings, boundary lengths, duplicate column names), and constructors.

> **Architecture update:** This flat `domain/` module will be split into entity-level modules (`task/`, `column/`, `board/`) each with their own `types.rs`, `service.rs`, and `mod.rs`. The types and validation logic implemented here will migrate to their respective entity modules. See `CONTEXT.md` for the target structure.
