---
title: "Light mode support and UI polish"
status: "ready-for-agent"
created: "2026-05-10T07:25:00Z"
updated: "2026-05-10T07:28:00Z"
labels: ["mvp", "enhancement", "ready-for-agent", "module:ui"]
---

## Parent

MVP PRD: `.scratch/mvp/prd.md`

## What to build

UI defaults to dark mode. Add toggle to switch between dark and light modes. Persist theme preference. Includes clean minimal design, responsive layout for different screen sizes, and smooth animations (for drag-drop and UI transitions).

## Acceptance criteria

- [ ] App defaults to dark mode on first launch
- [ ] Theme toggle button in UI (dark/light)
- [ ] Theme preference persisted to local storage
- [ ] Clean, minimalistic design throughout
- [ ] Responsive layout adapts to window size
- [ ] Smooth animations for: drag-drop operations, modal open/close, theme transition
- [ ] TDD cycles for theme toggle, persistence, defaults (see Agent Brief)

## Blocked by

- `.scratch/mvp/06-task-crud.md`
- `.scratch/mvp/04-column-service.md`
- `.scratch/mvp/07-drag-and-drop.md`

---

> *This was generated by AI during triage.*

## Agent Brief

**Goal:** Add light/dark mode toggle, persist theme preference, and polish the overall UI.

**What to build:**
- App defaults to dark mode on first launch
- Theme toggle button in header/navbar
- Theme preference persisted to `localStorage` (key: `tasks-mini-theme`, values: `"dark"` | `"light"`)
- On load: read from localStorage, default to dark if not set
- CSS: use Tailwind `dark:` variant for theme switching
- Add `dark` class to `<html>` element for dark mode, remove for light mode

**UI polish (across all components):**
- Clean, minimalistic design with consistent spacing
- Responsive layout: columns scroll horizontally on narrow windows, stack on mobile
- Smooth animations: CSS `transition` on modal open/close (opacity + transform), theme transition (background-color, color)
- Modal transitions: fade in/out with slight scale

**Implementation notes:**
- Frontend only — this is a Leptos/TailwindCSS task
- Use `window().local_storage()` to persist theme
- Leptos `create_effect` to sync theme class on `<html>` element
- Tailwind config: ensure `darkMode: 'class'` is set in `tailwind.config.js`
- Replace placeholder Tauri+Leptos welcome page with actual Kanban board layout
- Board layout: horizontal scrollable row of columns, each column has header + task list

**Dependencies:** None new — uses existing TailwindCSS + Leptos

**TDD Cycles** (execute one at a time, RED→GREEN→REFACTOR):
1. `app defaults to dark mode on first launch` → dark class on html element → no refactor
2. `theme toggle switches dark class on html element` → toggle button + class swap → extract theme signal
3. `theme preference persists in localStorage` → localStorage read/write on toggle → no refactor
4. `theme restored from localStorage on load` → load-time check → no refactor
5. `modal open/close uses CSS transition` → transition on opacity + transform → no refactor
6. `theme transition animates background and color` → CSS transition on theme change → no refactor
