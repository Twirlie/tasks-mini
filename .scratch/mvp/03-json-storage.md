---
title: "JSON file storage adapter"
status: "completed"
created: "2026-05-10T07:25:00Z"
updated: "2026-05-11T15:20:00Z"
labels: ["mvp", "enhancement", "completed", "module:json-storage"]
---

## Parent

MVP PRD: `.scratch/mvp/prd.md`

## What to build

Implement the `Storage` trait using JSON file persistence. Store data in `~/.local/share/tasks-mini/workflow/`. Include schema versioning in JSON files. Auto-create backups before writes, keeping last 5 backups per workflow.

## Acceptance criteria

- [ ] Implement `Storage` trait for JSON files
- [ ] Data directory: `~/.local/share/tasks-mini/workflow/`
- [ ] Main file: `tasks.json` with schema_version field
- [ ] Auto-backup: `tasks.json.backup.<timestamp>` before every write
- [ ] Keep last 5 backups, delete older ones
- [ ] Handle corrupt files gracefully with error propagation
- [ ] TDD cycles for integration tests using temp directories (see Agent Brief)

## Blocked by

- `.scratch/mvp/02-storage-port.md`

---

> *This was generated by AI during triage.*

## Agent Brief

**Goal:** Create the `json_storage` module in `src-tauri/src/json_storage/` implementing the `Storage` trait with JSON file persistence.

**Key behaviors:**
- Data directory: `~/.local/share/tasks-mini/workflow/` (use `dirs::data_dir()` + `tasks-mini/workflow`)
- Main file: `tasks.json` containing the full `Board` serialized as JSON
- Schema versioning: `Board.schema_version` field in JSON
- Auto-backup: copy `tasks.json` to `tasks.json.backup.<timestamp>` before every write
- Backup rotation: keep last 5 backups, delete older ones
- Corrupt file handling: return `StorageError::Serialization` if JSON parse fails

**Implementation notes:**
- Implement `Storage` trait from `crate::storage_port`
- Import domain types from `crate::task::types`, `crate::column::types`, `crate::board::types`
- Use `tokio::fs` for async file operations (Tauri uses tokio runtime)
- Create data directory on init if it doesn't exist
- Use `chrono::Utc::now().timestamp()` for backup timestamps
- On `load_board()`: if file doesn't exist, return a default board with 4 columns (Backlog, Todo, In Progress, Done)
- On `save_board()`: backup first, then write atomically (write to temp file, rename)
- `create_task`/`update_task`/`delete_task`: load board, mutate, save board
- Same pattern for column operations

**Dependencies to add:**
- `dirs = "6"`
- `tokio = { version = "1", features = ["fs"] }`

**TDD Cycles** (execute one at a time, RED→GREEN→REFACTOR):
1. `save_board then load_board roundtrips Board data` → implement save_board + load_board with tokio::fs → no refactor
2. `load_board returns default board when file missing` → default board creation on missing file → extract default_board() helper
3. `save_board creates backup before write` → backup creation → no refactor
4. `save_board rotates backups keeping last 5` → backup rotation → extract rotate_backups helper
5. `load_board returns Serialization error on corrupt JSON` → corrupt file handling → no refactor
6. `schema_version persisted and read back correctly` → schema versioning → no refactor

**Test dependency:** `tempfile = "3"` (dev-dependency)

---

## Implementation Progress

**Status:** ✅ Complete

**Completed TDD Cycles:**
- ✅ Cycle 1: save_board then load_board roundtrips Board data
- ✅ Cycle 2: load_board returns default board when file missing
- ✅ Cycle 3: save_board creates backup before write
- ✅ Cycle 4: save_board rotates backups keeping last 5
- ✅ Cycle 5: load_board returns Serialization error on corrupt JSON
- ✅ Cycle 6: schema_version persisted and read back correctly

**Completed Storage Trait Methods:**
- ✅ create_task
- ✅ update_task
- ✅ delete_task
- ✅ create_column
- ✅ update_column
- ✅ delete_column

**Files Modified:**
- `src-tauri/Cargo.toml` - Added dirs, tempfile dependencies
- `src-tauri/src/json_storage/mod.rs` - Full implementation with all Storage trait methods
- `src-tauri/src/lib.rs` - Added json_storage module
- `src-tauri/src/board/types.rs` - Added tasks field to Board struct

**Dependencies Added:**
- `dirs = "6"`
- `tempfile = "3"` (dev-dependency)

**Test Results:**
All 53 tests passing, including 12 json_storage integration tests using temp directories.
