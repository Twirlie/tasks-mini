---
title: "Extract Domain Types Module"
status: "ready-for-agent"
created: "2025-06-17T12:00:00Z"
updated: "2026-05-11T17:52:00Z"
labels: ["frontend", "refactoring", "domain-types"]
---

## What to build

Extract the domain types (Board, Column, Task structs) from the monolithic `src/app.rs` file into a dedicated `src/domain/` module. This creates a clean separation between data structures and UI logic, making the codebase more maintainable and testable.

## Acceptance criteria

- [ ] Create `src/domain/mod.rs` with re-exports
- [ ] Create `src/domain/types.rs` containing Board, Column, Task structs with serde serialization
- [ ] Update `src/app.rs` to import domain types from the new module
- [ ] Add unit tests for domain type serialization/deserialization
- [ ] Verify the application still compiles and runs after refactoring

## Blocked by

None - can start immediately

## User stories covered

1. As a developer, I want domain types to be separated from UI logic, so that I can easily understand data structures without wading through component code.
9. As a developer, I want domain types grouped together, so that I can see all data structures in one place without file fragmentation.
10. As a developer, I want the modular structure to avoid over-engineering, so that the file organization remains manageable for a small application.

## TDD Cycles

1. `test domain type serialization` → extract Board struct with serde derives → verify JSON round-trip works
2. `test domain type deserialization` → extract Column and Task structs → ensure all types serialize correctly  
3. `test module imports` → create mod.rs with re-exports → verify app.rs can import and use types
4. `test compilation` → update imports in app.rs → ensure application builds and runs successfully

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Extract serde domain types (Board, Column, Task) from the monolithic app component into a dedicated domain module.

**Current behavior:**
The frontend `Board`, `Column`, and `Task` structs are defined inline in the main app component file, co-located with UI logic. This makes it difficult to understand data structures independently and prevents reuse across modules.

**Desired behavior:**
A dedicated `domain` module exists containing all serde-deserializable frontend data transfer objects. The main app component imports these types through clean module re-exports. The module uses the `mod.rs` pattern with a `types.rs` file to match the backend structure.

**Key interfaces:**
- `Board` — must remain `Serialize` + `Deserialize` with fields: `id`, `name`, `columns: Vec<Column>`, `tasks: Vec<Task>`, `schema_version: u32`
- `Column` — must remain `Serialize` + `Deserialize` + `Clone` + `Debug` with fields: `id`, `name`, `order`
- `Task` — must remain `Serialize` + `Deserialize` + `Clone` + `Debug` with fields: `id`, `title`, `description: Option<String>`, `column_id`, `order`, `created_at: String`, `updated_at: String`, `completed_at: Option<String>`
- `mod.rs` should re-export all types so consumers import from `crate::domain::*`

**Acceptance criteria:**
- [ ] A `domain` module exists with `mod.rs` re-exporting types
- [ ] `types.rs` contains `Board`, `Column`, `Task` with correct serde derives
- [ ] The main app component imports domain types from the new module (no inline struct definitions)
- [ ] Unit tests verify JSON round-trip serialization/deserialization for all three types
- [ ] The application compiles successfully after the refactor

**Out of scope:**
- Changing struct fields or types
- Adding business logic or validation to domain types (they remain simple DTOs)
- Backend code changes
- Moving UI components or utility functions

**TDD Cycles** (execute one at a time, RED→GREEN→REFACTOR):
1. `test Board serialization round-trip` → extract Board struct with serde derives → verify JSON round-trip works
2. `test Column and Task serialization round-trip` → extract Column and Task structs → ensure all types serialize correctly
3. `test module imports` → create mod.rs with re-exports → verify app.rs can import and use types
4. `test compilation` → update imports in app.rs → ensure application builds and runs successfully
